FROM node:22-alpine

WORKDIR /app

# Install dependencies first (layer cache)
COPY package.json package-lock.json* ./
RUN npm install

# Copy source (bind mount overrides in dev, but this ensures the image is self-contained)
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
